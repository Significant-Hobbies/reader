# Optional: server-side fallback key for Vercel AI Gateway
AI_GATEWAY_API_KEY=

# Optional: local AI endpoint for local AI mode
LOCAL_AI_URL=http://127.0.0.1:3456

# --- Turso (libSQL) ---
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# --- Auth.js (NextAuth v5) ---
# Generate with: openssl rand -base64 32
AUTH_SECRET=
# Base URL Auth.js uses for OAuth callbacks (e.g. http://localhost:3000 in dev,
# https://your-domain.com in prod). Optional in dev when `trustHost` is on.
NEXTAUTH_URL=
# Google OAuth client credentials (GCP Console > APIs & Services > Credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Vercel Blob (PDF storage) ---
BLOB_READ_WRITE_TOKEN=

# Recommended when running Playwright in serverless environments
# PLAYWRIGHT_BROWSERS_PATH=0
