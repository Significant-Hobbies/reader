import { ExtensionConnectClient } from '../../components/ExtensionConnectClient';

export const dynamic = 'force-dynamic';

export default function ExtensionPage() {
  return <ExtensionConnectClient />;
}
